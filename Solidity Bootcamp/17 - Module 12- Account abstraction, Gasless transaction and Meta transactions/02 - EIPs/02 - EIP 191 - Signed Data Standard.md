# EIP 191 : Signed Data Standard

This EIP explains a standard format for **signed data** in Ethereum contracts by prefixing data with `0x19` and a version byte, thereby preventing ambiguity with transactions and enabling versioning (e.g. personal\_sign, structured data).

Doc:<https://eips.ethereum.org/EIPS/eip-191>
