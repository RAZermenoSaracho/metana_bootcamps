# EIP 2771

This EIP explains a **standard protocol** (ERC-2771) that lets smart contracts accept *meta-transactions* via a **trusted forwarder**, so a user can sign off-chain while a relayer pays the gas.

<https://eips.ethereum.org/EIPS/eip-2771>
